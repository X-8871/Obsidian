---
type: guide
domain: vault
status: active
updated: 2026-09-02
aliases:
  - Obsidian Vault README
  - AI 全局整理规则
tags:
  - Obsidian
  - README
  - Vault
  - 知识库治理
---

# Obsidian Vault README · 全局规则与笔记指南

> [!INFO] 本文件的职责
> 这是 `C:\Users\22061\Documents\Obsidian Vault` 的根目录 README，也是人类与 AI 协作维护本知识库时的全局规则入口。
> 日常导航以 [[00-索引/00-总索引|00-总索引]] 为准；目录结构和元数据分别参考 [[00-索引/结构说明|结构说明]]、[[00-索引/类型规范|类型规范]]。

## 快速入口

- [[00-索引/00-总索引|知识库总索引]]：全库导航和当前主线。
- [[00-索引/项目索引|项目索引]]：项目主文档、计划、协作和技术沉淀。
- [[00-索引/课程索引|课程索引]]：课程、复习资料和习题入口。
- [[00-索引/AI与工作流索引|AI 与工作流索引]]：AI 工具、工作流和技能导航。
- [[09-AI_Knowledge_Hub/00-AI知识中枢总览|AI 知识中枢]]：跨工具规则、基础设施、项目维护手册和 SOP。
- [[08-PlanMind/00-INDEX|PlanMind 目标中心]]：年度、月度、周度、日度和长期目标。
- [[00-索引/Daily索引|Daily 索引]]：日记、每日记录与复盘。
- [[00-索引/电脑信息索引|电脑信息索引]]：本机环境、命令和调试记录。

## 目录与笔记介绍

| 目录 | 主要内容 | 新内容如何归档 |
|---|---|---|
| `00-索引` | 全库入口、MOC、分类索引、结构和类型规范 | 只放导航与治理说明，不堆放正文资料 |
| `01-Daily` | 日记、每日事实、复盘和临时任务记录 | 历史笔记按 `YYYY-MM/YYYY-MM-DD.md` 归档；插件生成路径按当前配置处理 |
| `02-项目` | 项目主文档、赛题、计划、分工、MVP 和项目知识 | 每个项目至少保留一篇主文档，并从 [[00-索引/项目索引]] 建入口 |
| `03-知识库` | 可跨项目复用的概念、技术、学习路线和工作流 | 从项目或课程中提炼出的稳定知识放在这里 |
| `04-课程` | 学校课程、复习资料、试卷、习题和课件 | 每门课程建立 `00-[课程名]复习总览.md`，模块按认知顺序编号 |
| `05-代码` | 代码说明、源码片段和模块文档 | 代码必须放入带语言标记的 Markdown 代码块，避免渲染断层 |
| `06-个人` | 个人规划、单词、健身和隐私资料 | 默认不读取隐私笔记，只有用户本轮明确点名后才处理 |
| `07-电脑信息` | 本机配置、命令入口和调试记录 | 环境操作前优先查看 [[07-电脑信息/命令直通]] 与 [[07-电脑信息/我的电脑信息]] |
| `08-PlanMind` | 当前唯一的目标与计划数据目录 | 通过 [[08-PlanMind/00-INDEX]] 进入，不绕过索引批量写入，也不要重新创建 `08-PLAN` |
| `09-AI_Knowledge_Hub` | AI 跨工具规则、基础设施运维、项目 Runbook、SOP 和私密隔离区 | 新文档必须回填 [[09-AI_Knowledge_Hub/00-AI知识中枢总览]] |
| `10-实验记录` | 上板、部署、量化链路和故障诊断记录 | 保留日期、环境、步骤、结果和可复现证据 |
| `Temptlaters` | Templater 与 QuickAdd 正在引用的模板 | 不擅自重命名或迁移，避免插件路径失效 |

> [!WARNING] 当前兼容提示
> - `08-PLAN` 是旧默认值导致的历史目录，已停用并清理；PlanMind 的唯一数据目录是 `08-PlanMind`。
> - 历史 Daily 已按月份归档，但 QuickAdd/Templater 可能仍引用 `01-Daily/Daily`、`01-Daily/Weekly`。未经用户明确授权，不修改 `.obsidian` 插件配置。

## 常见笔记类型

| 类型 | 用途 | 典型入口或位置 |
|---|---|---|
| MOC / 索引 | 聚合一个主题的入口，只做导航和简述 | `00-索引`、各课程 `00-...总览.md` |
| 项目主文档 | 描述目标、范围、架构、状态和关键链接 | `02-项目` |
| 项目计划 | 记录里程碑、MVP、分工和验收标准 | `02-项目/项目文档` 或项目目录 |
| 概念笔记 | 沉淀可复用的技术知识和因果解释 | `03-知识库` |
| 课程笔记 | 课程知识、习题、试卷和复习路线 | `04-课程` |
| 工作流 / SOP | 描述可重复执行的输入、步骤、输出和验收 | `09-AI_Knowledge_Hub/04-Workflows_SOP` |
| 维护说明 / Runbook | 记录项目路径、启动部署、红线和故障排查 | `09-AI_Knowledge_Hub/03-Project_Runbooks` |
| 实验记录 | 记录环境、过程、结果、失败原因和下一步 | `10-实验记录` |
| Daily | 记录当天事实、任务和复盘，不代替长期知识 | `01-Daily` |

## 全局协作规则

### 1. 修改范围与安全

1. 默认只读；写入前说明“改哪个文件、改什么、为什么”，并取得用户同意。
2. 只修改用户指定范围，不自发批量整理、重命名、移动或删除文件。
3. 不修改 `.obsidian`、插件配置和模板路径，除非用户明确点名。
4. 不覆盖用户已有的无关改动；操作前检查目标文件和 Git 工作区状态。
5. 禁止在 Vault 中保存密码、API Key、Token、私钥正文等敏感信息。
6. `06-个人/个人信息.md` 等隐私笔记默认不读取；`09-AI_Knowledge_Hub/05-Private_Vault` 仅在用户本轮明确授权后访问。
7. 禁止在 Vault 内生成临时脚本、索引数据库、日志、构建产物等 AI 工作文件；临时产物应放在 Vault 外部工作目录。

### 2. 编码、语言与时间

- 所有文件始终使用 UTF-8 读取、创建和写入。
- 代码注释和文档字符串统一使用中文。
- 默认时区为东八区（南京时间，UTC+8），默认语言环境为简体中文（zh-CN）。
- AI 回复用户时，第一句严格为 `Hello Wong.`。
- 修改项目代码后，必须核对本轮差异并完成必要验证；只提交本轮相关文件，不混入无关改动、密钥、构建产物或临时文件，并报告提交哈希。提交失败时必须说明原因。

### 3. 命名与归档

1. 笔记名称优先使用清晰、具体的中文；必须保留英文专有名词时可中英结合。
2. 易跨学科重名的文件添加领域或课程前缀，例如 `电工_21-22A卷答案.md`。
3. 需要固定阅读顺序的目录和模块使用 `00-`、`01-`、`02-` 数字前缀；不为没有顺序关系的知识卡片强行编号。
4. 新项目放入 `02-项目`，可复用知识放入 `03-知识库`，课程资料放入 `04-课程`，实验过程放入 `10-实验记录`。
5. 新建、移动或重命名重要笔记后，必须更新对应 MOC 或索引，避免产生孤岛笔记。

### 4. Frontmatter

重要且需要长期维护的笔记应使用标准 YAML Frontmatter；临时摘录和普通 Daily 不强制补齐。字段优先遵循 [[00-索引/类型规范|类型规范]] 和所属目录已有约定。

```yaml
---
type: guide
domain: vault
status: active
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags:
  - 核心标签
---
```

- `type` 描述笔记职责，例如 `moc`、`project`、`concept`、`workflow`、`guide`、`standard`、`plan`。
- `domain` 描述所属领域，例如 `vault`、`course`、`embedded`、`ai-workflow`、`personal`。
- `status` 使用 `active`、`draft` 或 `archived`。
- 不为已有笔记批量注入 Frontmatter；新增字段前先对齐现有类型规范。

### 5. Markdown 与双链

1. 文档只保留一个一级标题，内容按二级、三级标题组织。
2. 使用 `[[笔记名]]` 或 `[[目录/笔记名|显示名称]]` 建立内部双链；Vault 内禁止使用 `file:///` 绝对链接。
3. 使用 `> [!NOTE]`、`> [!TIP]`、`> [!WARNING]`、`> [!IMPORTANT]` 强调重点，不滥用 Callout。
4. 代码必须放在带语言标记的三反引号代码块中；代码内的 `#` 不得被误渲染成标题。
5. 修改双链、文件名或路径时必须检查反向链接，避免破坏现有知识网络。

### 6. 课程资料与附件

1. 每个课程根目录建立 `00-[课程名]复习总览.md`，并按学习顺序为模块目录添加数字前缀。
2. DOCX、纯文本等文字型资料提取核心内容后重组 Markdown 层级，并保留来源说明。
3. 公式密集、图表密集或扫描型 PDF 不强行转换为纯文本；优先逐页生成清晰图片，存入课程内的 `附件` 目录，再用 `![[图片名.png]]` 组成长卷笔记。
4. 大型 PDF、PPT 和参考资料复制到所属主题的 `附件` 目录，通过 `[[文件名.pdf]]` 等内部链接访问，保证知识库可迁移。
5. 图片默认不自动旋转；只有用户明确要求，或文件明确属于手写卷面且方向显著错误时才纠正方向。
6. 附件名称应具体、可排序，例如 `电工习题_01.jpg`，避免 `图片1.png` 等无语义名称。

## 标准维护流程

### 修改前

- 阅读本 README、目标目录的 MOC 和相关维护说明。
- 确认目标路径、文件编码、链接关系和当前 Git 状态。
- 对重命名、移动、删除和覆盖操作明确列出影响范围；未获授权不执行。

### 修改中

- 采用最小修改，保持现有分类体系和写作风格。
- 不改用户未指定的正文，不清理看似多余但用途不明的文件。
- 对公式、链接、代码块、Frontmatter 和附件引用进行结构校验。

### 修改后

- 重新读取目标文件，确认 UTF-8、Markdown 结构和内部链接正确。
- 新建、移动或重命名笔记时同步更新相关索引或 MOC。
- 汇报修改文件、验证结果、未处理事项和风险。
- 涉及项目代码时完成必要测试并提交本轮相关差异；Vault 中已有的无关 Git 变更不得混入提交。

## 本机环境入口

- Vault 路径：`C:\Users\22061\Documents\Obsidian Vault`
- Python：使用 `python` 命令；涉及 ESP-IDF 等独立工具链时，以对应项目维护说明为准。
- Windows 路径包含中文或空格时必须使用引号或 `-LiteralPath`。
- PowerShell、Git Bash 与项目工具链的命令差异，以 [[07-电脑信息/命令直通|命令直通]] 中已验证写法为准，不默认使用 WSL 命令。
- `reg`、`wmic` 等系统命令受限时，优先使用 Python 标准库或明确可用的 Windows 接口。

## 维护入口

- [[09-AI_Knowledge_Hub/03-Project_Runbooks/Obsidian_知识库运维与维护说明|Obsidian 知识库运维与维护说明]]
- [[09-AI_Knowledge_Hub/04-Workflows_SOP/Obsidian课程资料整理SOP|Obsidian 课程资料整理 SOP]]
- [[00-索引/结构说明|目录结构说明]]
- [[00-索引/类型规范|笔记类型规范]]

> [!IMPORTANT] 最终原则
> 先理解，再修改；只改授权范围；重要笔记必须可导航、可追溯、可迁移；任何无法确认用途的目录、链接或插件配置都不得擅自处理。

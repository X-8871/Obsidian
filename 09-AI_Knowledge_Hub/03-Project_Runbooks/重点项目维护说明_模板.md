---
type: project-runbook-template
domain: project-governance
status: template
created: 2026-08-26
tags: [Template, Project, Runbook, Architecture]
---

# 📦 [项目名称] 运维与维护手册 (模板)

> [!IMPORTANT]
> 任何 AI 在接手、重构或部署本项目前，必须通读本手册，并**严格遵守第三节「架构红线与禁忌」**。

---

## 一、 项目物理地址与架构概览

- **本地绝对物理路径 (Absolute Path)**: `C:/Users/22061/...`（必填，精准定位）
- **远程 Git 仓库地址 (Remote Repo)**: `git@github.com:username/repo.git`
- **核心编程语言 & 版本**: Python 3.10 / Node.js 20 / C++17
- **关键框架与三方依赖**: [如 FastAPI / Vue3 / PyTorch]
- **核心目录结构说明**:
  ```text
  📁 project-root/
  ├── 📁 src/core/       <-- 核心业务逻辑与算法
  ├── 📁 src/api/        <-- 对外接口层
  ├── 📁 tests/          <-- 单元与集成测试
  └── 📁 config/         <-- 配置文件与环境变量
  ```

---

## 二、 运行与部署指令 (Runbook)

### 1. 本地开发环境拉起
```bash
# 安装依赖
pip install -r requirements.txt

# 启动本地服务
python main.py
```

### 2. 测试与代码校验
```bash
# 运行单元测试
pytest tests/ -v
```

### 3. 构建打包与发布
```bash
# 构建镜像或打包
docker build -t project-name:latest .
```

---

## 三、 🚫 架构红线与禁忌 (Architectural Redlines)

> [!CAUTION]
> **以下为绝对禁止擅自修改的架构红线，除非得到用户明确指示：**

1. **红线 1**：[例如：认证鉴权流程禁止改为无状态 Token，必须保留现有校验网关]
2. **红线 2**：[例如：禁止升级某核心依赖版本，存在底层 C 扩展兼容性破坏]
3. **红线 3**：[例如：核心数据模型字段名称禁止重命名，影响下游历史数据库表]

---

## 四、 故障排查与已知问题记录

| 典型问题/报错现象 | 根本原因 | 标准排查与恢复手段 |
| :--- | :--- | :--- |
| `端口被占用` | 残留后台进程未退出 | 执行 `netstat -ano \| findstr :端口` 并杀进程 |
| `数据库连接超时` | 连接池耗尽或网络策略 | 检查连接池上限与健康检查配置 |

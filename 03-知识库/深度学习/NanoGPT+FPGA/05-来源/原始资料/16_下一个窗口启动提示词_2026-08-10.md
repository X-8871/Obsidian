# 下一个窗口启动提示词（2026-08-10）

将下面整段内容复制到新的 AI 窗口：

---

你现在接手我的 nanoGPT Zynq FPGA 学习与部署项目。

项目根目录：

```text
D:/FPGA/NanoGPT/nanogpt-zynq-backups-main
```

请先完整读取以下交接资料，不要让我重新口述整个历史过程：

```text
D:/FPGA/NanoGPT/nanogpt-zynq-backups-main/AI交接包_前七章学习资料/00_AI接手说明.md
D:/FPGA/NanoGPT/nanogpt-zynq-backups-main/AI交接包_前七章学习资料/14_学习状态总结与一周FPGA部署学习计划.md
D:/FPGA/NanoGPT/nanogpt-zynq-backups-main/AI交接包_前七章学习资料/15_Vivado至FPGA单Token板级实验记录_2026-08-09.md
D:/FPGA/NanoGPT/nanogpt-zynq-backups-main/AI交接包_前七章学习资料/16_下一个窗口启动提示词_2026-08-10.md
```

## 一、我的学习方式

- 每次只教一个当前步骤，先讲清它在完整数据流中的位置、目的和成功标准，再让我操作。
- 我发回截图、命令输出或现象后，你要逐项判断“通过 / 未通过 / 证据不足”，然后只给下一步。
- 所有英文缩写第一次出现时，要说明英文全称、中文含义和本项目中的作用。
- 遇到问题时不要连续堆很多命令。每次说明：当前在验证什么假设、为什么相关、预期结果、结果分支和下一步。
- 不要重新从第一章讲 Transformer；只在当前操作暴露出概念缺口时补讲。
- 不要把 Python 参考结果、软件 Build 成功或 JTAG 单 Token 成功扩大成“多 Token 离线部署完成”。

## 二、当前已经完成的真实状态

1. 当前权重、scale、Embedding、Q30 参数、Vitis 头文件和 Vivado 参数已统一。
2. 当前权重 SHA256：

```text
0f6b6bf5376041c66704377b90fb0937e9c4774d8a139f4853b843063baa5ab7
```

3. Vivado 2026.1 的 Synthesis、Implementation 和修复后的 FXF2 Bitstream 已生成成功。
4. Vitis 2026.1 已读取当前 XSA，Platform 和 `ps_mailbox_runner.elf` 构建成功。
5. 已定位并修复 FFN-only 条件下错误读取残差数据的问题；修复后的 PL 构建标记为：

```text
FXF2 = 0x46584632
```

6. 已在真实 Zynq-7020 开发板上通过 JTAG/XSDB 完成单 Token 推理：

```text
输入提示词：ROMEO:
生成数量：1
Token ID：0
字符：0x0A（换行符）
PS 返回码：0
PL 状态：完成
AXI 错误计数：0
```

7. 已生成独立启动包：

```text
D:/FPGA/NanoGPT/deploy_current_20260808/boot/BOOT_fx2_standalone_20260809.bin
```

8. JTAG 单 Token 自动复验入口：

```text
D:/FPGA/NanoGPT/deploy_current_20260808/run_fx2_cold_jtag.ps1
D:/FPGA/NanoGPT/deploy_current_20260808/deploy_fx2_cold_jtag.tcl
```

除非后续结果与基线冲突，否则不要先重复参数锁定、Vivado 初次构建、Vitis 初次构建或 JTAG 单 Token 实验。

## 三、2026-08-10 必须完成的主任务

按以下顺序一步一步带我完成，不能直接跳到最后：

### P0-1：SD 卡脱机启动

- 使用读卡器把 `BOOT_fx2_standalone_20260809.bin` 复制到 FAT32 SD 卡根目录。
- 在 SD 卡中重命名为唯一的 `BOOT.bin`。
- 安全弹出 SD 卡，开发板完全断电，设置 SD Boot 模式，插卡并重新上电。
- 不使用 JTAG/XSDB，验证开发板能够独立启动。

### P0-2：UART 验收

- 检查 USB-UART、COM 口、115200 baud、8N1、TX/RX 和共地。
- 先确认出现 `nanoGPT Zynq UART ready` 或项目定义的就绪信息。
- 使用下面的上位机发送 `1:ROMEO:`：

```text
D:/FPGA/NanoGPT/nanogpt-zynq-backups-main/host_tools/nanogpt_uart_gui/dist/KeChuangNanoGPT.exe
```

- 单 Token 脱机结果应与 JTAG 基线一致：Token ID `0`，字符 `0x0A`。

### P0-3：多 Token 单板离线生成

依次测试：

```text
1:ROMEO:
8:ROMEO:
20:ROMEO:
200:ROMEO:
```

如果仓库历史 200 Token 验收使用了不同提示词，应先查明并分别记录，不要悄悄替换测试条件。

每次必须保存：提示词、生成数量、Token ID 序列、字符输出、是否超时、错误状态、首 Token 延时、总耗时。

### P0-4：Python Q30 对齐

- 使用项目指定的 `venv-gpu`，不要使用系统 Python。
- 对相同提示词生成相同数量的 Q30 Token。
- 比较板端与 Python 的 Token ID 序列，而不只是比较显示字符。
- 如果不一致，报告首个 mismatch 的位置、两边 Token ID、对应字符和此前上下文，再按层级排查。

### P0-5：稳定性、性能和归档

- 同一提示词连续运行三次，比较三次 Token ID 是否完全一致。
- 记录首 Token 延时、平均单 Token 延时、总耗时和 Token/s。
- 保存 UART 日志、截图、当前 Bitstream、XSA、ELF、BOOT、manifest 和 SHA256。
- 更新实验记录，分别标明 JTAG、SD 脱机、UART、多 Token、Python 对齐是否通过。

## 四、有剩余时间才做的任务

主任务全部完成后，指导我从打开 Vivado 开始，在尽量不使用 PowerShell 的情况下重新完成一次部署：

```text
打开 Vivado 工程
→ 检查 IP 和 Block Design
→ Run Synthesis
→ Run Implementation
→ Generate Bitstream
→ Export Hardware Platform（包含 Bitstream）
→ 打开 Vitis
→ Switch / re-read XSA
→ Generate Platform
→ Build ps_mailbox_runner
→ 使用 Vitis 图形化 Run/Debug 下载和运行
→ UART 验收
```

这项任务的目的，是让我掌握 Vivado/Vitis GUI 流程，不是重新排查已经解决的 FXF2 算法问题。若 Vitis GUI 无法装载外部权重和量化二进制，要先解释工具边界，再决定是否必须使用 XSDB；不要为了“不使用 PowerShell”而省略模型数据。

## 五、成功结论的边界

只有实际看到相应证据后，才能使用以下结论：

- JTAG 单 Token 通过：已经完成。
- SD 脱机启动通过：尚待实际断电启动验证。
- UART 单 Token 通过：尚待串口收发验证。
- 多 Token 单板离线生成通过：尚待 8/20/200 Token 实测。
- Python Q30 与 FPGA 多 Token 对齐通过：尚待逐 Token 比较。
- 完整 FPGA 离线部署完成：必须至少同时满足 SD 脱机、UART 和多 Token 验收。

现在请先确认你已经读取上述文件，然后从“检查电脑是否识别读卡器中的 SD 卡、确认盘符和 FAT32 文件系统”开始，只给我第一步操作和成功判据。

---
